# Package assets

此目录保存公开、可版本化且不参与业务程序集编译的包资产。

- `schemas/`：跨进程信封和 `.lwpkg` manifest 的 JSON Schema；修改须配套契约测试。
- `examples/`：只允许自制、获授权或最小合成样本；不得提交来源不明壁纸。
- `web-runtime/`：版本化的 Web Bridge 与兼容脚本；不得包含包专属数据。

