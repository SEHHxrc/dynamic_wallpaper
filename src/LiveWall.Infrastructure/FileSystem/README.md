# FileSystem

提供受限路径解析、原子文件替换、随机临时目录和安全清理。所有路径操作需确保目标位于明确根目录内。

- `CanonicalPathResolver` 只接受以 `/` 分隔的包内相对路径，拒绝路径穿越、Windows 保留名和现存重解析点。
- `AtomicFileWriter` 在目标同目录写入并刷盘临时文件，再以替换或移动提交；配置和状态文件不得直接覆盖写入。
